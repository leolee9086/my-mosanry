# 瀑布流组件设计思考 (Masonry Component Design Notes)

> 灵感来源：[masonic](https://github.com/jaredLunde/masonic) - 一个优秀的 React 瀑布流库

## 1. 衡量标准：一个优秀的瀑布流组件应该具备什么？

一个通用、高性能的瀑布流组件，其优劣主要体现在以下几个核心方面：

### 1.1. 性能 (Performance) - 基石

性能是虚拟列表的生命线，尤其是在处理海量数据时。

- **[P0] 渲染性能**：
  - **虚拟化**：只渲染视口内（Viewport）的元素，这是最基本的要求。
  - **高效查询**：必须有高效的算法能快速找出视口内的元素。我们目前使用的 **R-Tree** 是一种优秀的方案。`masonic` 使用的 **区间树** 也是一个值得考虑的备选方案，特别是在列数固定的情况下。
  - **滚动占位**：在快速滚动时，对新出现的元素使用预估高度的占位符进行渲染，等滚动停止后再渲染真实内容，避免滚动过程中因内容渲染复杂导致的卡顿。

- **[P1] 滚动性能**：
  - **滚动事件节流（Throttling）**：避免在高频滚动中频繁触发计算，这是必须的。
  - **`isScrolling` 状态**：向子组件提供一个 `isScrolling` 标志。这允许子组件在快速滚动时进行降级渲染（比如暂时不加载图片、不播放视频、不做复杂动画），极大提升滚动流畅度。
  - **被动事件监听（Passive Event Listeners）**：在绑定 `scroll` 事件时，明确使用 `{ passive: true }`，告知浏览器该事件监听器不会调用 `preventDefault()`，让浏览器可以不必等待监听器执行完成就去执行滚动，优化滚动性能。

- **[P2] 更新性能**：
  - **尺寸动态更新**：当图片加载完成、或内容动态变化导致卡片高度改变时，布局需要更新。这个更新过程应该是高效的、局部的，而不是粗暴地重新计算所有布局。
  - **批量更新（Batching）**：在短时间内收到多个更新请求时，应将它们合并为一次处理，避免不必要的重复计算。我们目前的 `setTimeout` 机制就是一种实现。

### 1.2. API 设计 (API Design) - 灵魂

简洁、直观、灵活的 API 是组件能否被开发者接受和喜爱的关键。

- **[P0] 核心 Props**：
  - `items`: 数据源，类型清晰。
  - `columnWidth`: 基础列宽，用于响应式计算。
  - `gap` / `columnGutter` / `rowGutter`: 清晰的间距控制。
  - `idKey` / `itemKey`: 用于提高渲染性能和稳定性的唯一标识。

- **[P1] 渲染控制**：
  - `itemHeightEstimate`: **（我们目前缺失）** 预估行高。这对 SSR（服务端渲染）、快速滚动占位、以及更精确的首屏渲染计算至关重要。
  - `overscanBy`: **（我们目前缺失）** 控制视口外预渲染的区域大小。允许开发者根据应用场景在"滚动流畅性"和"单次渲染开销"之间做权衡。
  - `render` / `v-slot`: 提供作用域插槽，将 `item` 数据、`index`、`isScrolling` 状态等回传给调用者，让渲染完全自定义。

- **[P2] 交互控制**：
  - `scrollToIndex`: **（我们目前缺失）** 一个非常实用的功能，允许程序控制列表滚动到指定的数据项。
  - `onRender`: **（我们目前缺失）** 当渲染的条目范围变化时触发的回调，方便做曝光统计等。

### 1.3. 功能完备性 (Feature Completeness) - 血肉

除了基础的瀑布流，还应能优雅地处理各种常见场景。

- **[P0] 无限加载**：滚动到底部时，能方便地集成加载更多的逻辑。我们目前的 `@load-more` 事件是一种不错的实现。`masonic` 的 `useInfiniteLoader` Hook 是另一种更解耦的思路。
- **[P1] 响应式布局**：容器宽度变化时，能自动调整列数并重新布局。
- **[P2] 空状态和加载状态**：当 `items` 数组为空，或正在加载时，能够方便地展示占位内容。
- **[P2] 滚动容器自定义**：支持在指定 `div` 元素内滚动，而不仅仅是 `window`。

### 1.4. 可扩展性与定制化 (Extensibility & Customization) - 风骨

对于高级用户，应提供"逃生舱"，让他们能突破默认实现，组合出更复杂的功能。

- **[P0] 模块化导出**：像 `masonic` 一样，将内部的核心逻辑（如 `usePositioner`, `useScroller` 等）作为独立的 Composable 函数导出。这让用户可以完全绕开我们的主组件，用这些基础零件搭建自己的特殊布局。
- **[P1] HTML 标签定制**：允许用户通过 `as` 或 `itemAs` 这样的 prop 来指定渲染的根元素和列表项元素的 HTML 标签（`div`, `ul`, `li`...），以满足不同的语义化需求。

## 2. 我们的改进路线图 (Roadmap)

基于以上分析，我们可以为 `my-masonry` 制定一个清晰的改进计划：

1.  **第一阶段：架构重构（关注点分离）**
    - [ ] **拆分 `useVirtualization`**：创建 `useScrollObserver.ts`，专门处理滚动监听、`isScrolling` 状态和无限加载逻辑。
    - [ ] **改造 `useVirtualization`**：使其变得纯粹，只负责根据输入的视口范围，调用布局引擎的搜索方法。
    - [ ] **更新 `VirtualMasonryGrid`**：将新的 Composable 组装起来。

2.  **第二阶段：API 增强**
    - [ ] **添加 `isScrolling` 状态**：并将其通过作用域插槽暴露出去。
    - [ ] **添加 `itemHeightEstimate` Prop**：用于优化初始渲染和滚动占位。
    - [ ] **添加 `overscanBy` Prop**：允许精细控制预渲染范围。
    - [ ] **实现 `scrollToIndex` Prop**：提供程序化滚动能力。

3.  **第三阶段：走向开源品质**
    - [ ] **导出核心 Composable**：将内部的 `use...` 函数全部导出，供高级用户使用。
    - [ ] **完善文档**：为每个 Prop 和 Composable 编写清晰的 JSDoc 注释。
    - [ ] **添加更多示例**：例如"滚动到指定项"、"自定义滚动容器"等。

这个路线图能确保我们一步一个脚印地把组件打磨得越来越好。 